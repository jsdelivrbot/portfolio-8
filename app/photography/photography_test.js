'use strict';

describe('portfolio.photography module', function() {

  beforeEach(module('portfolio.photography'));

  describe('photography controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var photographyCtrl = $controller('photographyCtrl');
      expect(photographyCtrl).toBeDefined();
    }));

  });
});